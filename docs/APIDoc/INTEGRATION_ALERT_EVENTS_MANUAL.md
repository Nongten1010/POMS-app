# Integration Alert Events API

เอกสารนี้ย้ายไปยัง canonical backend documentation แล้ว:

- [Integration Alert Events](../backend/api/integrations/alert-events/README.md)
- [API breaking-change log](../backend/api/CHANGELOG.md)

ให้ใช้ canonical page เป็น contract ปัจจุบัน โดย request ใช้ `eventDate` และ `time` รูปแบบ `HH:00` แทน `startTime` และ `endTime`
